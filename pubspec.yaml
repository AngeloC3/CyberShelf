name: cybershelf
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  drift:
  path:
  path_provider:
  http:
  flutter_riverpod:
  riverpod_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev:
  build_runner:
  riverpod_generator:

flutter:
  uses-material-design: true