name: cybershelf
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  drift:
  path:
  path_provider:
  http:
  flutter_dotenv:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev:
  build_runner:
  mockito:

flutter:
  uses-material-design: true
  assets:
    - .env